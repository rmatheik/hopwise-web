import { FJ } from '..';
import { Command } from 'commander';
interface Options {
    config: string;
}
export declare function syncProject(options: Options, program: Command): Promise<void>;
export interface ProjectSyncMeta {
    configPath: string;
    config: FJ.FireJetConfigResolved;
    program: Command;
}
export {};
