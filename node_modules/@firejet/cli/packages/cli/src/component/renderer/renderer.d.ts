import { InlineConfig } from 'vite';
import { type HtmlToFigmaSuccess } from '@firejet-sync/shared/types';
import { RenderPermutation } from '../addComponent/getRenderInfo';
export declare function renderComponentFromVite(viteConfig: InlineConfig, renderPermutations: RenderPermutation[]): Promise<{
    img: Buffer;
    figmaVal: HtmlToFigmaSuccess;
    permutation: RenderPermutation;
}[] | null>;
