import { FJ } from '@firejet-sync/shared/types';
/**
 * The goal of this function to create a bundle for each group where the components can be imported
 * The bundle should be a single JS file where the components can be imported from
 *
 */
export declare function createComponentLibrary(rootFolderPath: string, renderFolderPath: string, componentEntryPath: string, groupName: string, group: FJ.FireJetConfigGroupResolved): Promise<{
    groupId: string;
}>;
