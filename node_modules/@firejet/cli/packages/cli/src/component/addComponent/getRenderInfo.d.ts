import { PropCombination } from '@firejet-sync/shared/types';
import { FJ } from '../..';
import { PopulateRenderComponentDirOpts } from '../renderFileCreation/projectFileHandling';
import { ExtraClassStyles } from '../renderFileCreation/propsGeneration';
export interface RenderPermutation {
    componentName: string;
    groupName: string;
    component: FJ.ComponentDef;
    propCombi: PropCombination;
    /**A stringified version of the component in JSX to be used for rendering */
    componentJsx: string;
    extraClassStyles: ExtraClassStyles;
    id: string;
}
export declare function getRenderPermutations(groupName: string, group: FJ.FireJetConfigGroupResolved): RenderPermutation[];
export declare function getCssImportString(opts: PopulateRenderComponentDirOpts): string;
export declare function getProjectFramework(opts: PopulateRenderComponentDirOpts): void;
