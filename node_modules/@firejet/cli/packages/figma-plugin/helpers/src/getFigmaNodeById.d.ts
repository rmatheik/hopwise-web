/**Use this function instead of the default getFigmaNodeById because figma has some weird internal edgecase with our custom ids */
export declare function getFigmaNodeById(id: string | undefined): BaseNode | null;
