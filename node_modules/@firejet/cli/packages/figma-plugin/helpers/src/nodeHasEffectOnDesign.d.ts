/**Some nodes exist purely for grouping aesthetically, like groups or frames with no backgrounds, we will not include these in our output */
import { CustomSceneNode } from '@firejet-sync/shared/types';
export declare function nodeHasEffectOnDesignShallow(node: SceneNode | CustomSceneNode): boolean;
export declare function nodeHasEffectOnDesign(node: SceneNode): boolean;
