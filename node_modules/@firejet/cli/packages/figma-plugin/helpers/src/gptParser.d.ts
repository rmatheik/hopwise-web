import { GptResult, GptSchemaBase } from '@firejet-sync/shared/types';
export declare const GPT_ACTIONS_INSTRUCTIONS_PROMPT = "The output needs to be machine parseable so do not deviate from the schema provided below.\nFirst start with one of the available CAPITALISED action names, followed by a newline\nEach next line, until the next action name will be values sepeated by commas, to perform the action defined at the start.\nNone of the values should be surrounded by quotes.\nnodeIds should strictly be just a parseable integer.\nThere should be no leading or trailing whitespace, proceed straight to the comma.\n\nExample:\nACTION\nval1,val2,some text here\nval3,val4,some other text here\n\nACTION_2\nval5\nval6\n";
export declare function getSystemPrompt(systemPrompt: string, schema: GptSchemaBase): string;
export declare class GptOutputParser<T extends GptSchemaBase> {
    private schema;
    private logger?;
    private currentAction;
    private result;
    constructor(schema: T, logger?: {
        log: (message: string) => void;
    } | undefined);
    reset(): void;
    parse(gptOutput: string): void;
    parseLines(lines: string[]): void;
    parseLine(line: string): void;
    finish(): GptResult<T>;
}
