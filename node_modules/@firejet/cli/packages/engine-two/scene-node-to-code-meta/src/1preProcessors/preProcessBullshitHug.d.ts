import { PreProcessorHandler } from '../handlers';
/**
 * This is required because figma has an edge case where empty auto layout frames can be set to hug
 * Which makes no sense from a CSS perspective as it will result in a width of 0
 * We should only do this for auto layout frames
 * See BullshitPurpleHug in e2e test cases as example
 */
export declare const preProcessBullshitHug: PreProcessorHandler;
