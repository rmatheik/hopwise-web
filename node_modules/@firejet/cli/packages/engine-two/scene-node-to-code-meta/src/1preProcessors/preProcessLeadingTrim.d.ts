import { PreProcessorHandler } from '../handlers';
/**
 * Figma has a leading trim property that shaves off the top and bottom of text.
 * This is not a valid css property (except in SAFARI imagine that)
 * As a fallback we use fixed height but and width which is not optimal
 */
export declare const preProcessLeadingTrim: PreProcessorHandler;
