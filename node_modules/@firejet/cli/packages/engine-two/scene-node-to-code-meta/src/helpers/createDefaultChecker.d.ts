import { CustomSceneNode } from '@firejet-sync/shared/types';
import { ScenePath } from '../ScenePath';
export declare function createDefaultChecker(key: keyof CustomSceneNode, isDefaultPredicate: (path: ScenePath) => boolean): (path: ScenePath /**A function that runs if the key is default */, onDefault?: () => void) => void;
