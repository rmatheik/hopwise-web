import { CustomSceneNode } from '@firejet-sync/shared/types';
import { Rectangle } from '@firejet-sync/shared/types';
export declare function childBoxesFitParent(parent: Rectangle, children: Rectangle[], side?: 'width' | 'height' | 'both', thresholdFactor?: number): boolean;
export declare function enoughSpaceForChildren(node: CustomSceneNode): boolean;
export declare function getSpaceForChildMeta(node: CustomSceneNode): {
    totalChildSpaceUsed: number;
    availableSpace: number;
    totalChildAndGapSpaceUsed: number;
};
