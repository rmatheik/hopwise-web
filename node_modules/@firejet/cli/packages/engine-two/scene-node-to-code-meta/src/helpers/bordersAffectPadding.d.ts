import { CustomSceneNode } from '@firejet-sync/shared/types';
export declare function bordersAffectPadding(node: CustomSceneNode): boolean;
interface Paddings {
    left: number;
    right: number;
    top: number;
    bottom: number;
}
/**We need padding adjustments because while figma does not consider borders in the box model, CSS does
 * So with paddings, the position of children is padding + borderWeight instead of just padding like in figma
 */
export declare function getPaddingAdjustmentFromStroke(node: CustomSceneNode): Paddings;
export {};
