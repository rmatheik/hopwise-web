import { CustomSceneNode } from '@firejet-sync/shared/types';
/**
 * Unskewed
 */
export declare function isFlippedTransform(node: CustomSceneNode): boolean;
/**
 *
 * Returns HORIZONTAL if ONLY horizontally flipped
 * Returns VERITCAL if ONLY VERITCALly flipped
 * returns HORIZONTAL_VERTICAL if both horizontal and vertically flipped
 * return undefined in all other cases
 */
export declare function getFlippedStatus(node: CustomSceneNode): 'HORIZONTAL' | 'VERTICAL' | 'HORIZONTAL_VERTICAL' | undefined;
