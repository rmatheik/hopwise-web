import { SizeVal } from '@firejet-sync/shared/types';
export declare const COMBINE_STRATEGY: {
    readonly backdropFilter: (vals: string[]) => string;
    readonly boxShadow: (vals: string[]) => string;
    readonly filter: (vals: string[]) => string;
    readonly top: (vals: (SizeVal | "auto")[]) => "auto" | SizeVal;
    readonly left: (vals: (SizeVal | "auto")[]) => "auto" | SizeVal;
    readonly right: (vals: (SizeVal | "auto")[]) => "auto" | SizeVal;
    readonly bottom: (vals: (SizeVal | "auto")[]) => "auto" | SizeVal;
    readonly transform: (vals: string[]) => string;
};
