export interface GridSpace {
    /**The size of the space where grid children can be for that axis */
    size: number;
    /**Whether the space contains grid children */
    containsItem: boolean;
}
export type RepeatGridPatternPredicate = (spaces: GridSpace[]) => boolean;
