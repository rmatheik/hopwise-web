import { UpdateMetaHandler } from '../handlers';
/**When there is not enough space on a node,
 * IF JUSTIFY BETWEEN: all the children except the first need to have negative margins
 * IF SINGLE CHILD: The child needs negative margins to breath
 * IF ANYTHING ELSE: The children MUST exceed the parent node - don't put negative margins*/
export declare const handleCrampedChildrenPrimaryAxis: UpdateMetaHandler;
