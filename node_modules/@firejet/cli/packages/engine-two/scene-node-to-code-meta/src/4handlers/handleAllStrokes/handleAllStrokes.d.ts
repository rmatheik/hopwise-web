import { UpdateMetaHandler } from '../../handlers';
export declare const handleAllStrokes: UpdateMetaHandler;
/**This does not produce a pixel perfect result - but it will try its best to at least deliver something */
export declare const bestEffortBorder: UpdateMetaHandler;
