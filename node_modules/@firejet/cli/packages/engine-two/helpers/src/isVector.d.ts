import { CustomSceneNode, ReplaceContentType } from '@firejet-sync/shared/types';
/**Returns undefined if we want the backend to decide */
export declare function getExportAs(node: CustomSceneNode): ReplaceContentType | undefined;
export declare function isVector(node: CustomSceneNode): boolean;
export declare function nodeIsCollage(node: CustomSceneNode): boolean;
export declare function isCollage(nodes: CustomSceneNode[]): boolean;
