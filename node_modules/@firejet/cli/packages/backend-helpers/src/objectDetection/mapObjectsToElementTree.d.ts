import { Quadtree, Rectangle as QTRectangle } from '@timohausmann/quadtree-ts';
import { LayoutElementMeta, MappedNonTextElement, DetectedObjects } from '@firejet-sync/shared/types';
export declare function mapObjectsToElementTree(objects: DetectedObjects, codeMetaRoot: LayoutElementMeta): {
    mappedRoot: MappedNonTextElement;
    quadTree: Quadtree<QTRectangle<number[]>>;
} | undefined;
export declare function findBestMatch<T>(quadTree: Quadtree<QTRectangle<T>>, rect: QTRectangle<T>): {
    iou: number;
    rect: QTRectangle<T>;
};
