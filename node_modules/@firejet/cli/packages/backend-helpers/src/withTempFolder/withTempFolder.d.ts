import { GeneratedFolder } from '@firejet-sync/shared/types';
interface FolderMeta {
    folderPath: string;
    folderPathPosix: string;
}
export declare function withTempFolder<T>(callback: (meta: FolderMeta) => Promise<T>, cleanup?: boolean): Promise<T>;
export declare function populateFolder(folderPath: string, folder: GeneratedFolder): Promise<void>;
export {};
