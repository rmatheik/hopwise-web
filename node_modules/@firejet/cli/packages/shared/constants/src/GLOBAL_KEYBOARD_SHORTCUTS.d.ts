export type GlobalKeyboardShortcuts = {
    [key: string]: {
        ctrlkey: boolean | 'both';
        altKey: boolean | 'both';
        shiftKey: boolean | 'both';
    };
};
/**For keyboard shortcuts that should be usable in both the editor as well as the main layout */
export declare const GLOBAL_KEYBOARD_SHORTCUTS: GlobalKeyboardShortcuts;
