export type FlagChances = {
    [key: string]: {
        [value: string]: number;
    };
};
export declare const flagChances: {
    oneTimePayment: {
        oneTimePayment: number;
        control: number;
    };
};
export type FeatureFlags = {
    [K in keyof typeof flagChances]: keyof (typeof flagChances)[K];
};
