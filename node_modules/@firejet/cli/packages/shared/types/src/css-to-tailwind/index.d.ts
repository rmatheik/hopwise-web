export interface TailwindResult {
    tailwindClasses: {
        [prefix: string]: string[];
    };
    unconvertedCss: CSSRule[];
}
export interface CSSRule {
    name: string;
    value: string;
    tailwindPrefix?: string;
}
