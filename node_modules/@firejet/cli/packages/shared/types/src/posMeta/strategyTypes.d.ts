import { PosMetaWithStrategy, PosStyle } from '@firejet-sync/shared/types';
export interface BaseStrategy {
    sortedChildren: PosMetaWithStrategy[];
    childStyles: PosStyle[];
    parentStyles: PosStyle;
}
export type AbsStrategy = BaseStrategy;
export type FlexStrategy = BaseStrategy;
export type CheckStratFunc = (posMeta: PosMetaWithStrategy, existingStyles: PosStyle) => FlexStrategy | undefined;
