import { ComponentData } from './componentData';
export interface FireJetSaveFile {
    pages: Record<string, PageData>;
    version: {
        major: number;
        minor: number;
        patch: number;
    };
}
export type PageData = {
    componentData: ComponentData[];
};
