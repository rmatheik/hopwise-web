import { FJCode } from '../files';
export type DeletedMixin = {
    deleted?: true;
};
export type LayoutMixin = {
    x: number;
    y: number;
    width: number | null;
    height: number | null;
};
export type FilePathMixin = {
    filePath: string;
    exportName: string;
};
export type FilesMixin = {
    files: Record<string, Omit<FJCode, 'ast'>>;
};
