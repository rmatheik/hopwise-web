export interface ScreenSize {
    size: number;
    failed?: boolean;
}
export interface SnapshotMismatch {
    expectedSimilarity: {
        old?: number;
        new: number;
    }[];
    screenSizes: ScreenSize[];
}
export type SnapshotMismatches = Record<string, SnapshotMismatch>;
