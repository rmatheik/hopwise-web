export interface TestCase {
    expected: Record<string, Uint8Array>;
    name: string;
    id: string;
    testCase: string;
    assets: {
        IMG: Record<string, Uint8Array>;
        SVG: Record<string, {
            name: string;
            svgString: string;
        }>;
    };
}
