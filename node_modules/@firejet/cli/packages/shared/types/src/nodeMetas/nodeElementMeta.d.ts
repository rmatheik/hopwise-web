import { FilePathMixin } from '../save-files';
import { Optional } from '../util';
export type NodeMeta = NodeElementMeta | NodeTextMeta;
export type NodeMetaPartial = NodeElementMetaPartial | NodeTextMeta;
export type NodeElementMetaPartial = Omit<Optional<NodeElementMeta, 'nodeId'>, 'children'> & {
    children: NodeMetaPartial[];
};
export declare class NodeElementMeta {
    readonly type = "ELEMENT";
    nodeId?: string;
    tag: string;
    attrs: Record<string, string>;
    style: Record<string, string | null>;
    children: NodeMeta[];
    file?: FilePathMixin;
    /**If we are importing a file maybe we want to display some skeleton before the actual component loads */
    childrenHint?: NodeMeta[];
}
export declare class NodeTextMeta {
    readonly type = "TEXT";
    text: string;
}
