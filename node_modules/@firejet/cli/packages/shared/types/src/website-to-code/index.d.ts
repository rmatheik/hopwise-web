import { MappedWebsiteComponent } from '../user-component';
interface PaidUser {
    id: string;
    paymentStatus: 'PAID';
}
interface UnpaidUser {
    id: string;
    paymentStatus: 'UNPAID';
    freeImportsLeft: number;
}
export type FigmaUserInfo = PaidUser | UnpaidUser;
export type CapturePageReq = Omit<MappedWebsiteComponent, 'img' | 'figmaNodes'>;
export {};
