export type AreEqual<T, U, IfTrue = true, IfFalse = false> = [T] extends [U] ? [U] extends [T] ? IfTrue : IfFalse : IfFalse;
export type AllKeysUsed<T, Keys> = AreEqual<T, Keys, undefined, Exclude<T, Keys> | Exclude<Keys, T>>;
