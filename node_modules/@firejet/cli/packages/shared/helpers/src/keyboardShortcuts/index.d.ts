export type KeyboardShortcutOpts = {
    ctrlKey: boolean;
    altKey: boolean;
    shiftKey: boolean;
    preventDefault: boolean;
    element?: HTMLElement | React.MutableRefObject<HTMLDivElement>;
    sandboxOnly: boolean;
    globalAction: boolean;
};
/**Only runs once. Make sure everything works on initial set. Also when used inside editor will only run on editor keypresses*/
export declare function useKeyboardShortcut(code: string, opts: Partial<KeyboardShortcutOpts>, action: (e: KeyboardEvent) => void, onAnyPress?: (e: KeyboardEvent) => void): void;
export declare function simulateKeypress(code: string, opts: Partial<KeyboardShortcutOpts>): void;
