import { Coord } from '../htmlElements';
export declare function addVec2D(vec1: Coord, vec2: Coord): {
    x: number;
    y: number;
};
export declare function minusVec2D(vec: Coord): {
    x: number;
    y: number;
};
