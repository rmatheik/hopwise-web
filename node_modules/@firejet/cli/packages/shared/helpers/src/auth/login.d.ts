import type { Session } from '@supabase/supabase-js';
export declare class TimeoutError extends Error {
}
export declare function setupLogin(): Promise<{
    readKey: string;
    loginUrl: URL;
} | null>;
export declare function finishLogin(readKey: string, shouldAbort: () => boolean, timeout: number): Promise<Session | undefined>;
