import type { Session } from '@supabase/supabase-js';
export declare function refreshLogin(accessToken: string, refreshToken: string): Promise<{
    success: true;
    userSession: Session;
} | {
    success: false;
    error: any;
}>;
