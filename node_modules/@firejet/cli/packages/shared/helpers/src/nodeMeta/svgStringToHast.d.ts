export type HastElement = {
    type: 'element';
    tagName: string;
    properties: Record<string, string>;
    children: (HastElement | string)[];
} | {
    type: 'text';
    value: string;
};
/**This has an edge case which does not work for multiple elements, only the first element will be converted properly */
export declare function svgStringToHast(source: string): HastElement;
