import { Rectangle } from '@firejet-sync/shared/types';
type RelativeStrategy = 'MIN' | 'MAX' | 'CENTER';
export type RelativeMeta = ReturnType<typeof getRelativePosition>;
/**This automatically tries to make the resulting positions symettrical and rounded */
export declare function getRelativePosition(relativeRect: Rectangle, referenceRect: Rectangle): {
    left: number;
    top: number;
    right: number;
    bottom: number;
    xCenterOffset: number;
    yCenterOffset: number;
    leftPercent: number;
    topPercent: number;
    rightPercent: number;
    bottomPercent: number;
    xStrategy: RelativeStrategy;
    yStrategy: RelativeStrategy;
};
export {};
