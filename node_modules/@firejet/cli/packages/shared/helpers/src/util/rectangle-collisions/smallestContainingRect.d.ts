import { Rectangle } from '@firejet-sync/shared/types';
/**Given an array of rectangles find the smallest rectangle that would contain all the rectangles */
export declare function getSmallestContainingRect<T extends Rectangle>(rects: T[]): Rectangle;
