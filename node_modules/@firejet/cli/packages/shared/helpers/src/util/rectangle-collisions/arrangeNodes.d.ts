import { Rectangle, Side } from '@firejet-sync/shared/types';
export declare function getArrangement(strategy: Side): 'HORIZONTAL' | 'VERTICAL';
export declare const arrangeNodes: <T extends Rectangle>(childCoords: T[], strategy: Side) => T[];
