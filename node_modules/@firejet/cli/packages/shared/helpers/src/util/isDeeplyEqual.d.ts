/**
 * @param  {T} a
 * @param  {T} b
 * @param  {string[]=[]} ignoreKeys The key must STILL exist, but the value can be different
 */
export declare function isDeeplyEqual<T>(a: T, b: T, ignoreKeys?: string[]): boolean;
