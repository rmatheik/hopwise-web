import { PosMeta, PosMetaExtended, Side } from '@firejet-sync/shared/types';
export declare const ALL_SIDES_NO_CENTER: Side[];
export declare const ALL_SIDES: Side[];
/**HCENTER means a horizontal line passes through the centers. VCENTER means a vertical line passes through the centers */
export declare function groupNodesBySameSide(posMetas: PosMeta[], side: Side): PosMetaExtended[][];
