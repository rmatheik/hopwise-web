interface DecomposedTransform {
    rotation: number;
    scale: [number, number];
    skew: [number, number];
}
type MtxTransform = [[number, number, number], [number, number, number]];
export declare function decomposeTransform(transform: MtxTransform): DecomposedTransform;
export {};
