import { CodeMeta } from '@firejet-sync/shared/types';
export declare class NameDeclasher {
    private usedNames;
    constructor(existingNames?: Iterable<string>);
    static fromComponents(codeMeta: CodeMeta): NameDeclasher;
    private getBaseName;
    declash(name: string): string;
    getDeclashedIfChanged(name: string): string | undefined;
}
