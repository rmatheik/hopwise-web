export declare function asEvalableStr(content: string): string;
export declare function isEvalableStr(content: string): boolean;
/**Checks if a string when resolved would be a single variable */
export declare function isVariableStr(content: string): boolean;
