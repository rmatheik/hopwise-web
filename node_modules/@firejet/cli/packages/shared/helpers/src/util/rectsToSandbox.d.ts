import { Rectangle } from '@firejet-sync/shared/types';
export type RectWithMeta = Rectangle & {
    color?: string;
    outlineColor?: string;
    tooltipContent?: string;
    content?: string;
    zIndex?: number;
    children?: RectWithMeta[];
};
export declare function rectsToTailwindPlay(rects: RectWithMeta[]): Promise<string>;
