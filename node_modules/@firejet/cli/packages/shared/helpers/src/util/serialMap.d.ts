/**Run a set of promises, but one by one instead of all at once.
 * Good for expensive operations that tend to crash computers */
export declare function serialMap<T, V>(arr: T[], func: (val: T) => Promise<V>): Promise<V[]>;
