import { Rectangle, Side } from '@firejet-sync/shared/types';
export declare function getDirection(strategy: Side): 'TOP_DOWN' | 'BOTTOM_UP' | 'LEFT_RIGHT' | 'RIGHT_LEFT';
export declare const getMargins: <T extends Rectangle>(group: T[], strategy: Side, shouldSort?: boolean) => {
    gap: number;
    margins: number[];
    secondaryAxisOffsets: number[];
    spaceTakenByNodes: number[];
    arrangedGroup: T[];
    sizeKey: "width" | "height";
    axisKey: "x" | "y";
    strategy: Side;
    noMarginsUsed: boolean;
};
