import { CodeMeta, LayoutElementMeta, LayoutMeta } from '@firejet-sync/shared/types';
/**The function is run FIRST before the children are traversed - feel free to remove/add children before traversing
 *
 * You can use the context to set a value at each level
 * The context will be deepcopied to each child
 * Edits to the context at each level will persist in children
 *
 */
export declare function traverseLayout(layout: LayoutMeta, func: (curLayout: LayoutMeta, parent: LayoutElementMeta | undefined, context: Record<string, any>, parentIndex?: number) => void, parent?: LayoutElementMeta, context?: Record<string, any>, parentIndex?: number): void;
/**The function is run FIRST before the children are traversed - feel free to remove/add children before traversing */
export declare function traverseCodeMeta(codeMeta: CodeMeta, func: (curLayout: LayoutMeta, parent: LayoutElementMeta | undefined, context: Record<string, any>) => void, context?: Record<string, any>): void;
