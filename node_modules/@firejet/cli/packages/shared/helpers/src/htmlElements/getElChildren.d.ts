export declare function getElChildren(el: Element): Element[];
export declare function getElChildNodes(el: Element): Node[];
/**We must use this instead of the default query selector because this handles iframe children */
export declare function querySelectorAll(el: Element | Document, query: string): Element[];
/**We must use this because the `Element` type is different within iframes vs parent documents */
export declare function instanceOf<T>(item: any, constructor: {
    new (...args: any[]): T;
}): item is T;
