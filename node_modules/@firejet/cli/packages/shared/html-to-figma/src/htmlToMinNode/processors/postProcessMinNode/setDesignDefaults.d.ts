import { MinNode } from '@firejet-sync/shared/types';
/**Without these, some of the custom scene node processors happily remove the nodes */
export declare function setDesignDefaults(minNode: MinNode): void;
