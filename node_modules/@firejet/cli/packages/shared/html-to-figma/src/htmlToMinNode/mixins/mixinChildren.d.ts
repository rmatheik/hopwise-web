import { MinNode, Rectangle } from '@firejet-sync/shared/types';
import { TreeBuilderContext } from '../buildTreeFromRoot';
/**Keys that do not change when passing to children */
export declare const treeBuilderCtxBaseKeys: readonly ["rootRect", "rootEl", "isValidTextParent", "processedMaskedNodes"];
export declare const treeBuilderCtxInheritedKeys: readonly ["parentRect", "minSharedMeta"];
export declare const mixinChildren: (el: Element, ctx: TreeBuilderContext, parentRectGetter: (el: Element) => Rectangle, childGetter: (el: Element) => Element[]) => Promise<{
    children: MinNode[];
}>;
