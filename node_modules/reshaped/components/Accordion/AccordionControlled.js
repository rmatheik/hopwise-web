"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import React from "react";
import { classNames } from "../../utilities/helpers.js";
import useElementId from "../../hooks/useElementId.js";
import AccordionContext from "./Accordion.context.js";
const AccordionControlled = (props) => {
    const { children, onToggle, active, iconPosition, iconSize, className, attributes } = props;
    const rootClassNames = classNames(className);
    const id = useElementId();
    const value = React.useMemo(() => ({
        triggerId: `${id}-trigger`,
        contentId: `${id}-content`,
        active,
        onToggle,
        iconPosition,
        iconSize,
    }), 
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [active]);
    return (_jsx("div", Object.assign({}, attributes, { className: rootClassNames, children: _jsx(AccordionContext.Provider, { value: value, children: children }) })));
};
export default AccordionControlled;
