"use client";
import { jsx as _jsx } from "react/jsx-runtime";
import React from "react";
import FlyoutControlled from "./FlyoutControlled.js";
const FlyoutUncontrolled = (props) => {
    const { defaultActive, onClose, onOpen } = props;
    const [active, setActive] = React.useState(defaultActive || false);
    const handleClose = () => {
        setActive(false);
        if (onClose)
            onClose();
    };
    const handleOpen = () => {
        setActive(true);
        if (onOpen)
            onOpen();
    };
    return (_jsx(FlyoutControlled, Object.assign({}, props, { defaultActive: undefined, active: active, onClose: handleClose, onOpen: handleOpen })));
};
export default FlyoutUncontrolled;
