import { jsx as _jsx } from "react/jsx-runtime";
import Text from "../Text/index.js";
import { classNames } from "../../utilities/helpers.js";
import s from "./Hotkey.module.css";
const Hotkey = (props) => {
    const { children, active, className, attributes } = props;
    const rootClassNames = classNames(s.root, active && s["--active"], className);
    return (_jsx(Text, { as: "kbd", variant: "caption-1", weight: "medium", color: "neutral-faded", className: rootClassNames, attributes: attributes, children: children }));
};
export default Hotkey;
