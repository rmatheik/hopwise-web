var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { jsx as _jsx } from "react/jsx-runtime";
import SliderControlled from "./SliderControlled.js";
import SliderUncontrolled from "./SliderUncontrolled.js";
const Slider = (props) => {
    const { min = 0, max = 100 } = props, rest = __rest(props, ["min", "max"]);
    if (("value" in props && props.value !== undefined) ||
        ("minValue" in props &&
            "maxValue" in props &&
            props.minValue !== undefined &&
            props.maxValue !== undefined)) {
        return _jsx(SliderControlled, Object.assign({}, rest, { min: min, max: max }));
    }
    return _jsx(SliderUncontrolled, Object.assign({}, rest, { min: min, max: max }));
};
export default Slider;
