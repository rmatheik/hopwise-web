"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// Level 1 is token types, level 2 is token values
// { color: { foreground: {} } }
// We don't merge token values
const MAX_LEVEL = 2;
const mergeDefinitions = (originalDefinition, newDefinition, currentLevel = 1) => {
    // Overwrite the token value
    if (currentLevel > MAX_LEVEL)
        return newDefinition;
    const mergedDefinition = Object.assign({}, originalDefinition);
    Object.keys(newDefinition).forEach((key) => {
        mergedDefinition[key] = mergeDefinitions(mergedDefinition[key], newDefinition[key], currentLevel + 1);
    });
    return mergedDefinition;
};
exports.default = mergeDefinitions;
